# docker-compose.override.yml.example
#
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2024-2026 David Kleinhans, Jade University of Applied Sciences
#
# This file demonstrates how to customize the ITSM Service Catalogue with your
# organization's branding (logo, styles, templates).
#
# HOW IT WORKS:
# =============
# The application stores customizable files in apps/itsm/user_files/:
#   - static/logos/     → Organization logos
#   - static/css/       → Custom stylesheets
#   - latex_templates/  → PDF export templates
#
# The repository contains empty placeholder directories. Your organization's
# files are placed in overrides/ and mounted directly into user_files/,
# replacing the defaults.
#
# This approach:
#   ✓ Makes it explicit what gets replaced (visible in this file)
#   ✓ Keeps organization files separate from the repository
#   ✓ Simplifies version control (overrides/ is gitignored)
#
# USAGE:
# ======
# 1. Copy this file:
#    cp docker-compose.override.yml.example docker-compose.override.yml
#
# 2. Place your files in overrides/ (mirrors the user_files structure):
#    overrides/
#    ├── static/
#    │   ├── logos/
#    │   │   └── logo.png           # Your logo
#    │   └── css/
#    │       └── custom.css         # Your styles (optional)
#    └── latex_templates/           # PDF templates (optional)
#
# 3. Uncomment the volume mounts below for what you want to override
#
# 4. Start the application:
#    docker-compose up -d
#
# Docker Compose automatically merges this file with docker-compose.yml.

services:
  itsm:
    volumes:
      # =======================================================================
      # LOGO OVERRIDE
      # =======================================================================
      # Mount your logo directory to replace the default logos
      # Put your logo in: overrides/static/logos/logo.png
      # Then set LOGO_FILENAME=logo.png in env/itsm.env
      #
      - ./overrides/static/logos:/app/user_files/static/logos:ro

      # =======================================================================
      # CUSTOM CSS (optional)
      # =======================================================================
      # Mount custom stylesheets for branding colors, fonts, etc.
      # Put your CSS in: overrides/static/css/custom.css
      #
      # - ./overrides/static/css:/app/user_files/static/css:ro

      # =======================================================================
      # CUSTOM IMAGES (optional)
      # =======================================================================
      # Mount additional images (banners, backgrounds, icons)
      # Put images in: overrides/static/images/
      #
      # - ./overrides/static/images:/app/user_files/static/images:ro

      # =======================================================================
      # LATEX TEMPLATES (optional)
      # =======================================================================
      # Mount custom LaTeX templates for PDF export customization
      # Put .tex files in: overrides/latex_templates/
      #
      # - ./overrides/latex_templates:/app/user_files/latex_templates:ro

# =============================================================================
# ALTERNATIVE: External File Location
# =============================================================================
# For centralized management across multiple deployments, mount from
# an external location:
#
# services:
#   itsm:
#     volumes:
#       - /opt/organization/logos:/app/user_files/static/logos:ro
#       - /opt/organization/css:/app/user_files/static/css:ro
#       - /opt/organization/latex:/app/user_files/latex_templates:ro
